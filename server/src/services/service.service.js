/**
 * ServiceService — справочник услуг (прейскурант).
 *
 * Будущие методы:
 * - listServices(filters)
 * - getServiceById(id)
 * - createService(payload)
 * - updateService(id, payload)
 * - deactivateService(id) — is_available + deactivated_at (без физического удаления)
 */
class ServiceService {}

module.exports = new ServiceService();
