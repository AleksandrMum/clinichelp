/**
 * PatientService — карточки пациентов (manager: CRUD/archive; doctor: read).
 *
 * Будущие методы:
 * - searchPatients(query, pagination)
 * - getPatientById(id, options)
 * - createPatient(payload)
 * - updatePatient(id, payload)
 * - archivePatient(id)
 */
class PatientService {}

module.exports = new PatientService();
