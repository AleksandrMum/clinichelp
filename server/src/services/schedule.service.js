/**
 * ScheduleService — базовое расписание (rules) и исключения (exceptions).
 *
 * Будущие методы:
 * - getDoctorScheduleView(doctorId, range, viewerRole)
 * - replaceWeeklyRules(doctorId, rules[])
 * - createException(doctorId, payload)
 * - updateException(exceptionId, payload)
 * - archiveException(exceptionId)
 */
class ScheduleService {}

module.exports = new ScheduleService();
