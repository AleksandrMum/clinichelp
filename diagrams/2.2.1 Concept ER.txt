@startuml Логическая модель БД
!theme plain
skinparam linetype ortho

entity "Пользователь" as users {
  * id
  --
  full_name
  login
  password_hash
  role
  phone
  is_active
}

entity "Пациент" as patients {
  * id
  --
  full_name
  phone
  birth_date
  notes
}

entity "Услуга" as services {
  * id
  --
  name
  duration_min
  price
  is_available
}

entity "ОбычноеРасписание" as schedule_rules {
  * id
  --
  doctor_id
  weekday
  start_time
  end_time
}

entity "УточнениеРасписания" as schedule_exceptions {
  * id
  --
  doctor_id
  start_at
  end_at
  exception_type
  comment
}

entity "ЗаписьНаПрием" as appointments {
  * id
  --
  patient_id
  doctor_id
  service_id
  start_at
  end_at
  status
  booked_price
  comment
}

entity "Аудит" as audit_log {
  * id
  --
  user_id
  action_type
  entity_type
  entity_id
  action_time
  details
}

' Описательные связи между сущностями
users ||--o{ schedule_rules : "имеет\nграфик"
users ||--o{ schedule_exceptions : "имеет\nисключения"
users ||---o{ audit_log : "хранит\nдействие"

patients ||--o{ appointments : "имеет\nзапись"
services ||--o{ appointments : "используется"
users ||---o{ appointments : "создает"

@enduml